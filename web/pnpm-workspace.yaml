# Single-package repo. pnpm 10 requires a non-empty `packages:` field whenever
# this file exists; `.` declares the root (web/) as the sole package.
packages:
  - "."

allowBuilds:
  esbuild: true
  sharp: true
  unrs-resolver: true

ignoredBuiltDependencies:
  - sharp
  - unrs-resolver
