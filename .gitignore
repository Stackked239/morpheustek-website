# ── Confidential client source — intentionally NOT published ──
# Raw meeting transcripts, the confidential ICP, and internal working notes.
/context/
/claudedocs/

# ── OS / editor ──
.DS_Store
Thumbs.db
*.swp

# ── Logs ──
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# ── Node / Next.js (web/ also has its own .gitignore) ──
node_modules/
.next/
.turbo/
.vercel/
*.tsbuildinfo
next-env.d.ts

# ── Env / secrets ──
.env
.env.*
!.env.example

# ── Tooling artifacts ──
.playwright-mcp/
coverage/
